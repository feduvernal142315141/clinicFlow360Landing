/**
 * Testimonials section — hidden until real testimonials exist (PEND-07).
 * Per spec §46: "ocultar sección" until real data.
 * Renders nothing. Uncomment when real quotes + authorization available.
 */
export function Testimonials() {
  return null
}
